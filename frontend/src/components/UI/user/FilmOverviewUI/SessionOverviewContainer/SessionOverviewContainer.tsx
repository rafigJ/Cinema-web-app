import React, {FC} from 'react';
import {ISession} from "../../../../../types/model/ISession";

interface SessionOverviewContainerProps {
    sessions: ISession[];
}

const SessionOverviewContainer: FC = () => {
    //todo
    return (
        <div>

        </div>
    );
};

export default SessionOverviewContainer;